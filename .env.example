# SouqView Mobile – copy to .env and set your values
# -----------------------------------------------------------------------------
# Required for Supabase (Auth, DB, Realtime)
# Get these from: Supabase Dashboard → Project Settings → API
# -----------------------------------------------------------------------------
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Backend base URL (including /api). For physical device: use your PC's LAN IP (e.g. http://192.168.0.214:5000/api).
# Backend must listen on 0.0.0.0 — see docs/backend-lan-setup.md
EXPO_PUBLIC_API_URL=http://localhost:5000/api

# -----------------------------------------------------------------------------
# Backend-only (set these in your backend .env, not in the mobile app):
# Twelve Data, Groq, DeepSeek – see README.
# -----------------------------------------------------------------------------
