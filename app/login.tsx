/**
 * Login route – renders the Apple-style LoginScreen (Sign in with Google).
 */
import { LoginScreen } from '../src/screens';

export default function LoginRoute() {
  return <LoginScreen />;
}
